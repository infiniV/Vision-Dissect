# Server Configuration
DEVICE=cpu
MAX_IMAGE_SIZE=10485760

# Model Configuration
DEPTH_ANYTHING_VARIANT=vits
YOLO_CONFIDENCE=0.5
USE_ONNX=true
DEFAULT_DEPTH_FORMAT=downsampled

# Warmup Configuration (comma-separated list)
WARMUP_MODELS=depthpro,yolo_detect
